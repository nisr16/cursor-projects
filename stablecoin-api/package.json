{
  "name": "stablecoin-api",
  "version": "1.0.0",
  "description": "Goal: Enable 30-second cross-border transfers for regional banks using stablecoin infrastructure.",
  "main": "server-unified.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server-unified.js",
    "build": "cd financial-dashboard && npm install && npm run build && cd .. && cp -r financial-dashboard/build dashboard-build",
    "dev": "node server.js",
    "clean": "rm -rf node_modules package-lock.json && npm install",
    "deploy": "npm run build && npx vercel --prod",
    "deploy:preview": "npm run build && npx vercel"
  },
  "keywords": [
    "stablecoin",
    "banking",
    "api",
    "transfers",
    "fintech"
  ],
  "author": "Development Team",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.11.0",
    "better-sqlite3": "^12.2.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "joi": "^17.11.0",
    "nodemailer": "^6.9.8",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "twilio": "^4.20.1",
    "winston": "^3.11.0",
    "ws": "^8.16.0"
  },
  "optionalDependencies": {
    "@railway/cli": "^4.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "vercel": "^44.7.2"
  }
}
